import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No hay configuraciones experimentales necesarias para turbopack
  // si la estructura de carpetas es correcta.
};

export default nextConfig;
